import React from "react";
import "./project.scss";

const Project = ({pageContext}) => {
  console.log(pageContext);
  return (
      <div className="project">
          <p className="project__sample">hello there boi</p>
      </div>
  );
};

export default Project;
